name: Trigger auto deployment for container-app-bf4xm6stvowhk

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container-app-bf4xm6stvowhk-AutoDeployTrigger-c505ddd7-e0b0-4f3c-98f2-b73dc51feca8.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPBF4XM6STVOWHK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPBF4XM6STVOWHK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPBF4XM6STVOWHK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistrybf4xm6stvowhk.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPBF4XM6STVOWHK_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPBF4XM6STVOWHK_REGISTRY_PASSWORD }}
          containerAppName: container-app-bf4xm6stvowhk
          resourceGroup: express-hello
          imageToBuild: containerregistrybf4xm6stvowhk.azurecr.io/container-app-bf4xm6stvowhk:${{ github.sha }}
          


